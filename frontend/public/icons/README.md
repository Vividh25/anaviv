# App icons

Drop the PWA icons here:

- `icon-192.png` — 192x192
- `icon-512.png` — 512x512

Referenced by `../manifest.json` and `index.html`. Use maskable-safe artwork
(keep the important content within the center ~80% safe zone).
